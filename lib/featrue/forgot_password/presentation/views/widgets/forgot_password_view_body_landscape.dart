import 'package:flutter/material.dart';

class ForgotPasswordViewBodyLandscape extends StatelessWidget {
  const ForgotPasswordViewBodyLandscape({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Forgot Password View Body Landscape'),
    );
  }
}
