import 'package:flutter/material.dart';

class ForgotPasswordViewBodyPortrait extends StatelessWidget {
  const ForgotPasswordViewBodyPortrait({super.key});

  /// This method returns a placeholder widget. It is intended to be overridden
  /// in the implementation class.
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Forgot Password View Body Portrait'),
    );
  }
}
